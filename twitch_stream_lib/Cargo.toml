[package]
name = "twitch_stream_lib"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["dylib"]

[dependencies]
# libc = "0.2"
# ffmpeg-next = {version="8.0"}
# image = "0.25"
# reqwest = {version = "0.12", default-features = false, features = ["json", "http2", "rustls-tls"]}
# tokio = { version = "1", features = ["full"] }
# anyhow = "1.0"
